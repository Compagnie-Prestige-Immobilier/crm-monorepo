ENABLED=true
GLPI_URL=https://glpi.cpi-chues.com
GLPI_APP_TOKEN=
GLPI_USER_TOKEN=
PROJECTS_JSON='[{"name":"crm","category_id":"1","repository":"https://github.com/Compagnie-Prestige-Immobilier/crm-monorepo.git","base_branch":"dev","markers":[],"protected_paths":[".github",".golangci.yml","tools/dev","lefthook.yml","ticket-solver","CLAUDE.md","AGENTS.md",".claude",".codex","package.json","knip.json","sql/migrations"],"verification_command":"for f in $(git ls-files --cached --others --exclude-standard \"*_test.go\"); do head -1 \"$f\" | grep -q \"^//go:build integration$\" || { echo \"$f sans tag integration\"; exit 1; }; done && tools/dev/plafonds.sh && sqlc generate && mkdir -p web/dist && touch web/dist/.keep && golangci-lint run ./... && go vet -tags integration ./... && go build ./... && pnpm install --frozen-lockfile && go run ./cmd/server -openapi > openapi.json && pnpm format:check && pnpm dead-code && pnpm --filter @crm/panel gen && pnpm --filter @crm/panel build && pnpm lint && pnpm typecheck"}]'
BREVO_API_KEY=
CLAUDE_CODE_OAUTH_TOKEN_0=
CLAUDE_CODE_OAUTH_TOKEN_1=
CODEX_ACCESS_TOKEN=
GITHUB_APP_ID=5027081
GITHUB_APP_PRIVATE_KEY=
POLL_INTERVAL_SECONDS=30
MAX_CONCURRENT_JOBS=2
JOB_TIMEOUT_MINUTES=120
WORK_ROOT=/work/jobs
CACHE_ROOT=/work/cache
STATE_DB=/work/tickets.sqlite3
LOG_LEVEL=INFO
KAIRO_ADMIN_TOKEN=
