# Le code généré est COMMITTÉ, *.g.dart compris — voir docs/adr/0002.
# Un développeur Android doit pouvoir ouvrir le projet sans Node ni Java, et le
# portillon anti-dérive ne peut être total que si tout est dans l'arbre.
#
# Ne rien ajouter ici qui couvre lib/.

.dart_tool/
.packages
build/
pubspec.lock
doc/api/
