{
  "extends": "./tsconfig.json",
  // `fake-workspace.ts` sert aux tests d'intégration : il vit à côté du code
  // qu'il double, mais n'a rien à faire dans l'artefact de production.
  "exclude": ["src/**/*.test.ts", "src/**/fake-*.ts", "src/openapi.ts", "dist", "node_modules"]
}
